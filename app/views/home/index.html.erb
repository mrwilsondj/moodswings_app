<!-- <h1>Home#index</h1>
<p>Find me in app/views/home/index.html.erb</p> -->
    <div class="container">
      <br/>
      <div class="bs-docs-section">
        <div class="page-header">
          <div class="row">
            <div class="col-lg-12">
              <h1 id="buttons">MOOD ANALYSIS</h1>
            </div>
          </div>
        </div>

<!-- jquery animation loader -->
        <div id="container">
      <div id="topLoader">
      </div>

      <button id="animateButton">Animate Loader</button>

       <script>
        $(function() {
          var $topLoader = $("#topLoader").percentageLoader({width: 256, height: 256, controllable : true, progress : 0.5, onProgressUpdate : function(val) {
              $topLoader.setValue(Math.round(val * 100.0));
            }});

          var topLoaderRunning = false;
          $("#animateButton").click(function() {
            if (topLoaderRunning) {
              return;
            }
            topLoaderRunning = true;
            $topLoader.setProgress(0);
            $topLoader.setValue('0kb');
            var kb = 0;
            var totalKb = 999;

            var animateFunc = function() {
              kb += 17;
              $topLoader.setProgress(kb / totalKb);
              $topLoader.setValue(kb.toString() + 'kb');

              if (kb < totalKb) {
                setTimeout(animateFunc, 25);
              } else {
                topLoaderRunning = false;
              }
            }

            setTimeout(animateFunc, 25);

          });
        });
      </script>
    </div>


        <div class="row">
          <div class="col-lg-12">
            <form method="GET" action="/tweets">
              <input type="text" name="username" />
              <input type="submit" value="ANALYZE a friend">
            </form>
          </div>
        </div>
      </div>
      <div class="row">
       <div class="col-lg-6">
            <h2 id="type-blockquotes"><%= link_to "Fetch and map MOOD tweets", map_display_index_path, :class=>"btn btn-primary" %></h2>
        </div>
       </div>

        <!-- Blockquotes -->

<!--         <div class="row">
          <div class="col-lg-6">
            <h2 id="type-blockquotes">FEELINGS!</h2>
          </div>
        </div> -->



        <!-- <div class="row">
          <div class="col-lg-6">
            <blockquote>
              <p>Why doesn't anything work damn it!!!!!? I think it has to do with an invalid or outdated tutorial as usual. Most likely, the culprit is inside the views folder/map_display folder/index.html.erb</p>
              <small>John Doe from <cite title="Source Title"> The WDI Corp</cite></small>
            </blockquote>
          </div> -->
          <!-- <div class="col-lg-6">
            <blockquote class="pull-right">
              <p>I want to see my moody tweets on a map</p>
              <small>SOPHIE LAFFONT from <cite title="Source Title">WDI LLC</cite></small>
            </blockquote>
          </div> -->
<!--         </div>
      </div>
</div> -->
